div.innerHTML = `<img src="${data.image || '/static/fallback.png'}" onerror="this.src='/static/fallback.png'" style="width:100%; border-radius:12px;">`;
if (data.attributes) {
  const registry = data.attributes.find(a => a.trait_type === 'Registry URI');
  if (registry && registry.value.includes('ipfs://')) {
    // Fetch registry JSON dari IPFS, tampilkan link NFT lain (sebagai anak)
  }
}
if (type === "logic-block") {
  div.innerHTML = `<button onclick="alert('Logic dijalankan!')">🔁 Jalankan Logic</button>`;
}
https://your-vercel.app/canvas?contract=0x...&tokenId=2
